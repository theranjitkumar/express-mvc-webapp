<h2>Welcome to {{title}} Page</h2>
<p>This is {{title}} content.</p>

{{#each services}}
<div class="service">
    <h3>{{this.title}}
        <a href="/services/{{this.slug}}" class="read-more">Read More</a>
    </h3>
    <p>{{this.description}}</p>
</div>
{{/each}}